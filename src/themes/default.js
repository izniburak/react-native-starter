const space = []

const sizes = {
  actionButton: 48,
}

const colors = {
  textDark: '#444',
  textMedium: '#888',
  textLight: '#ccc',
}

const radii = {
  normal: 8,
  full: 9999,
}

export default {
  space,
  radii,
  colors,
  sizes,
}
